Ordinal targets get treated as unordered categories or as real numbers, and both throw the
ordering away. Encoding the order into the labels turns it into structured classification, and a
CRF then learns every parameter jointly.
